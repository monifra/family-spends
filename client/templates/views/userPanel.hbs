<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Family Finances - User Panel</title>
        <link rel="stylesheet" href="/css/normalize.css">
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        {{>header}}
        <main>
            <div id="main-wrapper">
                <form id="data-changes-form">
                    <h2 id="adding-and-removing">Add Expense</h2>
                    <input type="number" value="100">
                    <a>Submit</a>
                    <a href="/">Cancel</a>
                </form>
                <div id="fixed-data">
                    <p>Family name: {{familyName}}</p>
                    <p>Family members:</p>
                    <ul>
                        {{#each familyMembers}}
                            <li>{{this}}</li>
                        {{/each}}
                    </ul>
                    <h3 id="savings-to-change">Savings: ${{savings}}</h3>
                </div>
            </div>
        </main>
    </body>
</html>