<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Family Finances - User Panel</title>
        <link rel="stylesheet" href="/css/normalize.css">
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        {{>header}}
        <main>
            <div id="main-wrapper">
                <div id="data-changes-form">
<!--            <form id="data-changes-form">-->
                    <h2 id="adding-and-removing">Add Expense</h2>
                    <input type="number" value="100" min="0" id="money">
                    <p id="error"></p>
                    <button id="submitExpensesButton" onClick="window.location.reload()">Submit</button>
                    <a class="cancel-button" href="/">Cancel</a>
<!--            </form>-->
                </div>
                <div id="fixed-data">
                    <p>Family name: {{familyName}}</p>
                    <p>Family members:</p>
                    <ul>
                        {{#each familyMembers}}
                            <li>{{this}}</li>
                        {{/each}}
                    </ul>
                    <h3 id="savings-to-change">Savings: $<span id="savingsNow">{{savings}}</span></h3>
                    <p id="familyId">{{familyId}}</p>
                </div>
            </div>
        </main>
        <script src="/js/app.js"></script>
    </body>
</html>